"""Phase 4+ executable scripts.

Each ``download_*.py`` is idempotent and safe to re-run. ``download_all.py``
orchestrates them sequentially. Other scripts (Phase 5+) land here as
training / evaluation entry points.
"""
