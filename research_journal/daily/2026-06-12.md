# 2026-06-12

## Ran
- Phase 10 full-system Streamlit UI live on Colab via cloudflared tunnel (after fighting localtunnel JS-chunk drops, a ModuleNotFoundError from sys.path, and a missing `grad-cam`/`rank_bm25` deps).
- Step-wise disease-model diagnosis: accuracy + Grad-CAM on all 3 cascade stages (PlantVillage, Paddy Doctor, PlantDoc), OLD vs R models, multiple Grad-CAM target layers.
- Built LP-FT trainer (`src/disease/train_lpft.py`) + notebook (`notebooks/phase5_lpft_plantdoc.ipynb`) + tests. LP-FT run started on Colab.
- Created the master ledger `EXPERIMENT_LOG.md`.

## Worked
- **Live demo runs end-to-end**: disease + soil predictions → Strategy-B query rewrite → grounded cited answer → highlighted chunks. Apple Scab leaf → 97–100% correct, query rewritten to "scorched leaves with whitish spots", real Vrikshayurveda citations.
- **Diagnosis nailed the root cause**: cascade is healthy through stages 1–2 (PlantVillage 99.8% leaf-attention, Paddy 97.0% lesion-attention) and breaks at stage 3 (PlantDoc full fine-tune: 72.3%, background attention). Confirmed the bias is real (persists across Grad-CAM layers), not a viz artifact.
- LP-FT freeze-logic unit tests pass (backbone byte-identical after a training step).

## Didn't work
- **Background randomization (R model) confirmed a net loss**: PlantVillage 99.8→90.7, PlantDoc 72.3→66.8, only 1.2→5.9% central-attention gain. Consistent with literature. Demoted to negative-result ablation.
- cloudflared needed to replace localtunnel (localtunnel kept failing to fetch Streamlit's lazy JS chunks).

## Decisions
- Build on the **OLD** disease cascade, not R.
- Disease fix order: **LP-FT first**, then detect-then-crop, then masking (only for attention story). Chosen after a deep literature review (LP-FT has 11–15pp field evidence; masking fixes attention not accuracy).
- Our 72.3% PlantDoc is at the published frontier (~74–78%) — contribution is the diagnosis + fix + system, not SOTA accuracy.

## Tomorrow / next
- Read LP-FT test-cell results: accuracy delta (LP-FT vs OLD) + Grad-CAM. If accuracy up + attention on leaf → that's the fix. Else → detect-then-crop.
- Record the LP-FT result in `EXPERIMENT_LOG.md` §6.
