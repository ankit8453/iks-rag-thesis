# 2026-06-13

## Ran
- Built `disease_diagnosis_summary.ipynb` for the supervisor — stage-wise hero figure (PV leaf ✅ → Paddy lesion ✅ → PlantDoc OLD background ❌ → C-PD fix ✅) + before/after + fruit+leaf demo + full "techniques tried" table.
- Wired the C-PD model + a pretrained YOLO leaf detector into Phase 9 (crop-first pipeline). New shared helper `src/disease/leaf_detect.py::LeafCropper`.
- Diagnosed and fixed the Grad-CAM corner artifact; ran a 5-method CAM comparison.
- Confirmed the model's healthy-vs-diseased ability.
- Rebuilt Phase 9 as the FINAL clean notebook.

## Worked
- **Healthy-vs-diseased is near-perfect** (16/16 health-status on a spot check). The 27-class top-1 ≈66.6% is mostly **within-crop disease-SUBTYPE** confusion (corn rust↔blight, potato early↔late), NOT healthy/diseased. → reframes 66.6% from "weak" to "works where it matters". Phase 9 Cell 5b now measures full-set healthy-vs-diseased accuracy.
- **Grad-CAM corner hotspots were a VISUALIZATION artifact**, not the model (Ankit was right). Fixed with `eigen_smooth=True` — cleanest of GradCAM / GradCAM+eigen / GradCAM++ / HiResCAM / EigenCAM.
- **YOLO leaf detector** (`foduucom/plant-leaf-detection-and-classification`) works at **conf=0.10** (0.25 missed leaves). It returned boxes for peach/grape/corn once installed.
- **"Heatmap only for disease" rule** (Ankit's idea): healthy leaf → no heatmap (clean "healthy" label); diseased → eigen Grad-CAM on the lesion. Removes the "is this red the disease or just the leaf?" confusion. Implemented as `disease_explain()`.
- Clean diseased demo images: **idx 248, 106, 22** (PlantDoc test) → clean lesion attention.

## Didn't work / corrected
- First YOLO test failed silently: `ultralytics` wasn't installed (Cell 2 with the dep had errored, Ankit reverted to the old Cell 2). Fix: install ultralytics in a SEPARATE pip call.
- Grad-CAM on a **healthy leaf is inherently diffuse** (no lesion to focus on) — not a bug; that's why we suppress the heatmap for healthy.
- Cropping HELPS cluttered images but HURTS already-leaf-centric ones (grape got worse when cropped) — so crop-first matters mainly for messy uploads.
- Phase 9 Cell 2 plain `git clone` hit the LFS quota (exit 128) → switched to `GIT_LFS_SKIP_SMUDGE=1` + clean re-clone (the pattern the other notebooks use).

## Decisions
- **Don't drop Grad-CAM** — it's the interpretability evidence answering the supervisor's concern — but **stop perfecting it**: lock 248/106/22 + eigen + the healthy-rule, then move on to the core advisory system.
- Phase 9 FINAL recipe: C-PD model + YOLO crop (conf 0.10) + eigen Grad-CAM + heatmap-only-for-disease + healthy-vs-diseased metric.

## Cell 10 fix (evening) — disease-gated advisory + Strategy B
- Ankit caught a real flaw: Cell 10 generated "organic treatment for <healthy leaf>" queries even for HEALTHY leaves (peach, grape) — nonsensical. Also it used Strategy A (weak template baseline, scores 0.001–0.011), hiding the Strategy-B novelty.
- Fix: **gate the advisory on disease** (healthy → "no treatment needed", no query/retrieval) + use **Strategy B** (LLM-mediated, the contribution) for diseased leaves → bridged query → retrieval → grounded cited advice. Kept all 3 samples so both behaviors are visible.
- Updated Cell 11 to handle healthy samples (note instead of chunk panel).
- Confirmed: **Llama is NOT retrained** — it's a frozen instruction-following model steered by prompts (Strategy B prompt + §17 grounding prompt); knowledge comes from retrieved corpus (RAG), not weights.

## Phase 10 UI rebuilt (evening) — crop-first + gated + futuristic
- Rewrote `app/` for the updated scenario: C-PD model (`iks-disease-plantdoc-crop`) + YOLO `LeafCropper` (conf 0.10) + eigen Grad-CAM + heatmap-only-for-disease + disease-gated advisory (healthy → "no treatment", diseased → Strategy B → grounded answer).
- `config.py`: DISEASE_MODEL_REPO→C-PD, HEALTHY_CLASSES set + `is_healthy()`, YOLO config, new title/notes. `loaders.py`: added `load_cropper` + `EngineBundle.cropper`. `streamlit_app.py`: full rewrite + modern dark/glassmorphism CSS (gradient hero, glass cards, status pills green/red).
- `src/explain/gradcam.py`: added `eigen_smooth` param to `_run_gradcam` + new `disease_gradcam_eigen()` (blocks[-2] + eigen — the validated clean recipe), shared by app + notebook.
- `notebooks/phase10_launch_ui.ipynb`: added ultralytics (separate pip call).
- Validated: app imports, `is_healthy` correct, streamlit_app + gradcam parse, 13 app tests + 26 explain/disease tests pass.

## Corn-rust corpus gap (confirmed earlier today)
- Corn rust → honest refusal ("corpus lacks corn") because Vrikshayurveda = tree science. Apple/tree diseases retrieve real treatments. CONTAGION pathway over-narrows the query ("spreading from adjacent plants") → use UNKNOWN/SOIL_DRIVEN for a rich answer. Corpus covers trees/fruit, not grains.

## Phase 10 UI bug — crop/disease disagreement in the query (caught by Ankit)
- On the live UI, a **corn** leaf (model: "Corn rust leaf" 98% ✅) produced a query asking about **rice** ("What rice crop symptoms resemble scorched leaves with whitish spots…"). Cause: the query used the **crop dropdown** (left on its default `rice`) for `crop_type` while the symptoms came from the corn-rust label → contradictory query → refusal.
- Fix: the disease label *already names the crop* ("Corn rust leaf" → corn). Added `app_config.crop_from_disease()` (first token, normalised: Bell_pepper→bell pepper, Soyabean→soybean) and the UI now defaults the crop dropdown to **"auto"** (derive from detection); explicit picks still override. `MultimodalContext.crop_type` + the status banner now use the detected crop.
- Note: corn still refuses (corpus = tree science, no grains) — that refusal is correct. The fix makes the *query consistent*; for a satisfying grounded answer demo apple scab.

## Next
- Push + run final Phase 9 (Run all) and Phase 10 UI on Colab.
- Phase 10 demo pairing: corn (honest refusal = integrity) + apple scab (real grounded treatment = novelty delivers).
