# Programming
# Python 3.11+ (not pip-installable; pin via environment)

# Deep learning [§22]
torch>=2.2,<2.4
torchvision>=0.17,<0.19
timm>=1.0.0
transformers>=4.40,<4.45
accelerate>=0.30
bitsandbytes>=0.43       # 4-bit quantization
peft>=0.11               # parameter-efficient fine-tuning

# Computer vision [§22]
albumentations>=1.4
pillow>=10.0
pytorch-grad-cam>=1.5
captum>=0.7

# RAG and NLP [§22]
langchain>=0.2,<0.3      # orchestration (note: only as scaffolding; primary pipeline is plain Python)
chromadb>=0.5
faiss-cpu>=1.8
sentence-transformers>=3.0
rank-bm25>=0.2
ragas>=0.1.10
nltk>=3.8
rouge-score>=0.1.2

# Evaluation [§22]
torchmetrics>=1.4
bert-score>=0.3.13
scikit-learn>=1.4

# Deployment (optional) [§22]
streamlit>=1.35
gradio>=4.30

# Numerical / data
numpy>=1.26,<2.0
pandas>=2.2
pyyaml>=6.0
tqdm>=4.66

# Configuration enforcement [PDF-implied §14, §37]
pydantic>=2.7            # enforces SoilConfig.disallowed_outputs, configs' strict-extra

# Test runner [PDF-implied §37 reproducibility tests]
pytest>=8.2
